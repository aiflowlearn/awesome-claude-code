# 实战任务 4.3: 进入 Planning mode

## 目标
让 Claude 先做规划再执行，避免"还没理解问题就开始写代码"。

## 操作
方式一：按 `Shift+Tab` 切换到 plan 模式。

方式二：输入：
```
/plan 重构用户模块，把 session 认证改为 JWT
```

Claude 会先研究代码库，然后输出一份详细计划。你可以审查计划，修改后再让它执行。

## 完成标准
Claude 输出实施计划并等待你的确认，而不是直接开始改代码。理解 plan 模式适合复杂决策和大规模重构。
