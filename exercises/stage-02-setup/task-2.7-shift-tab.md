# 实战任务 2.7: 用 Shift+Tab 切换权限模式

## 目标
快速切换 Claude Code 的操作权限级别。

## 操作
按下 `Shift+Tab` 键，观察底部的权限模式变化：
- `default` — 读操作自动，写操作需确认
- `acceptEdits` — 文件编辑自动批准
- `plan` — 只做研究和规划，不执行修改

## 完成标准
成功切换权限模式。理解不同模式适用于不同场景：日常开发用 default，大量编辑用 acceptEdits，复杂决策用 plan。
