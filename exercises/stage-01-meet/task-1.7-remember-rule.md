# 实战任务 1.7: 让 Claude 记住规则

## 目标
体验 Claude Code 的自动记忆功能。

## 操作
在 Claude Code 提示符中输入：
```
记住：这个项目使用 pnpm 而不是 npm
```

然后可以用 `/memory` 查看是否写入了记忆文件。

## 完成标准
确认 Claude 将规则写入了记忆文件。理解你可以通过自然语言让 Claude 记住项目规则，不需要手动编辑配置。
